name : xmonad-config
version : 0.1.0
dependencies :
  - base >= 4.14 && < 5
  - xmonad
  - xmonad-contrib
  - xmobar
  - X11
  - containers

executables :
  xmobar:
    main: /home/radusiviorel/.config/xmobar/xmobar.hs
    ghc-options:
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
  xmonad :
    main:  xmonad.hs
    ghc-options :
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
